# Spec: ac2112e6-0345-4fd4-87bf-9c53db8889f8

## Requirement
Fix UI dashboard task list status. It currently displays `[green]COMPLETE[/]`. It should read COMPLETE in the color green.

## Phases
- PLANNING
- EXECUTION
- VERIFICATION

## Root Cause
Streamlit's `col.write()` does **not** render Rich/BBCode syntax like `[green]COMPLETE[/]`. It outputs the raw BBCode tags literally as text. The code at `dashboard.py:135` and `components.py:85` both use `col_phase.write(f"[{color}]**{item['phase']}**[/]")`, which causes `[green]COMPLETE[/]` to appear verbatim instead of rendering "COMPLETE" in green.

## Tasks

### Task 1: Add a colored-text rendering utility

| Field   | Value                                                                                                   |
|---------|---------------------------------------------------------------------------------------------------------|
| Scope   | `spine/ui/utils.py`                                                                                     |
| What    | Add `colored_html(text, color)` that returns `<span style="color:{color}"><strong>{text}</strong></span>` |
| Why     | Centralizes rendering; all callers share one implementation. Pure function, easy to test.               |
| AC      | Function exists, takes `(text, color)`, returns valid HTML fragment with bold+colored text.            |

### Task 2: Fix `dashboard.py` inline phase rendering

| Field   | Value                                                                                                         |
|---------|---------------------------------------------------------------------------------------------------------------|
| Scope   | `spine/ui/dashboard.py:135`                                                                                   |
| What    | `col_phase.write(f"[{color}]**{item['phase']}**[/]")` → `col_phase.markdown(colored_html(item['phase'], color), unsafe_allow_html=True)` |
| Why     | Primary location where the user sees broken BBCode tags.                                                      |
| AC      | Dashboard shows "COMPLETE" in green text instead of `[green]COMPLETE[/]`.                                     |

### Task 3: Fix `components.py` work-item card phase rendering

| Field   | Value                                                                                                                    |
|---------|--------------------------------------------------------------------------------------------------------------------------|
| Scope   | `spine/ui/components.py:85`                                                                                              |
| What    | `col_phase.write(f"[{color}]**{item.get('phase', '')}**[/]")` → `col_phase.markdown(colored_html(item.get('phase', ''), color), unsafe_allow_html=True)` |
| Why     | `work_item_card()` has the same BBCode issue.                                                                           |
| AC      | Cards show colored phase text without literal BBCode tags.                                                               |

### Task 4: Clean up unused BBCode badge functions

| Field   | Value                                                                                               |
|---------|-----------------------------------------------------------------------------------------------------|
| Scope   | `spine/ui/components.py` — `phase_badge()` and `status_badge()`                                    |
| What    | Update to return HTML format, or annotate as deprecated. Currently dead code (zero callers).       |
| Why     | Dead code emits incompatible BBCode; misleading for readers.                                       |
| AC      | Functions no longer return Rich BBCode. Either updated to HTML or annotated deprecated.            |

### Task 5: Verify end-to-end

| Field   | Value                                                                                               |
|---------|-----------------------------------------------------------------------------------------------------|
| Scope   | Manual verification + UI rendering test                                                             |
| What    | Start Streamlit dashboard, confirm phase column displays colored text, verify no new warnings.     |
| Why     | Ensures changes work correctly without regressions or security warnings.                           |
| AC      | Dashboard renders all phases with correct colors — no literal BBCode tags visible anywhere.        |

## Execution Result

- **Status**: FAILED (ACP timed out after 300.0s)
- **Exit code**: -1
- **Files changed**: 0 (execution never completed)

---
*Generated at 2026-05-11T11:49:53.365333+00:00*
*Updated at 2026-05-11T21:40:00Z — state synced from checkpoint, spec reformatted*