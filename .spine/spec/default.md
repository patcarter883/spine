# Spec: default

## Requirement
Add safeguards so if 'spine init' is run in a repo that already has spine setup it will detect this and offer the options to completely reinitialise spine, reinitialise but keep configuration settings, do nothing.

## Phases
- PLANNING
- EXECUTION
- VERIFICATION

## Tasks

### setup
- Description: Setup environment

### implement
- Description: Implement core features

## Subphase Results

### ANALYZE
- subphase: ANALYZE
- agent_role: explorer
- status: success
- tasks: {'parse_requirement': {'status': 'success', 'result': '\n\n# 🔍 Requirement Analysis: `spine init` Safeguard & Reinit Options\n\n## 📋 Requirement Summary\nWhen `spine init` is executed in a repository that already contains a spine initialization, the command must:\n1. **Detect** the existing spine setup.\n2. **Prompt** the user with three explicit choices:\n   - Completely reinitialize spine\n   - Reinitialize while preserving existing configuration\n   - Do nothing (exit gracefully)\n3. **Prevent** accidental destructive actions while maintaining pipeline/automation compatibility.\n\n---\n\n## 🔑 Key Components & Logic Breakdown\n\n| Component | Description |\n|-----------|-------------|\n| **Detection Module** | Identifies whether a spine initialization exists in the target repo. |\n| **Prompt Interface** | CLI interaction layer presenting the 3 options with clear descriptions. |\n| **Execution Paths** | Branching logic mapped to each user selection (full reset, config-preserving reset, skip). |\n| **State/Config Manager** | Handles backup, preservation, restoration, or deletion of spine artifacts. |\n| **Non-Interactive Support** | Flags for CI/CD or automated environments to bypass prompts. |\n\n---\n\n## 🧭 Detection Strategy\nDefine a deterministic, fast, and reliable detection mechanism. Suggested markers:\n- **Primary**: Presence of a versioned config file (e.g., `.spine/config.yaml`, `.spine/version`)\n- **Secondary**: Presence of spine-specific directories/artifacts (e.g., `.spine/cache/`, `.spine/hooks/`)\n- **Tertiary**: Environment variables or CI/CD pipeline markers injected during init\n- **Return Format**: `DetectionResult { exists: bool, markers: list[str], version: str | null, config_path: str | null }`\n\n---\n\n## 💬 User Interaction & Flow\n```\n[spine] Detected existing spine initialization in this repository.\nWhat would you like to do?\n  1) Completely reinitialize spine (wipes all state & config)\n  2) Reinitialize while keeping current configuration settings\n  3) Do nothing and exit\n\n> [1/2/3] \n```\n**Design Notes:**\n- Support standard CLI prompt conventions (`yes/no`, `1/2/3`, arrow keys)\n- Validate input with retry loop\n- Provide explicit warnings for destructive options\n- Support non-interactive fallback flags: `--skip`, `--reinit`, `--reinit-config`\n\n---\n\n## 🗃️ State & Configuration Handling\n| Option | Behavior |\n|--------|----------|\n| **1. Full Reinitialize** | Delete all `.spine/` artifacts, reset to defaults, run fresh init flow. |\n| **2. Config-Preserving Reinit** | Backup config → wipe runtime/state artifacts → restore config → apply defaults to missing sections. |\n| **3. Do Nothing** | Log informational message, exit with `0` or `1` based on convention, no filesystem changes. |\n\n**Safety Considerations:**\n- Use atomic moves/copies for backups\n- Validate config integrity before/after restoration\n- Gracefully handle read-only or locked files\n- Log all destructive actions for auditability\n\n---\n\n## ⚠️ Risks & Edge Cases\n| Risk | Mitigation |\n|------|------------|\n| Partial/corrupted existing init | Detect incomplete state, offer repair or force options |\n| CI/CD pipeline execution | Enforce non-interactive flags; fail safely if flags missing |\n| Concurrent init runs | Implement file/lock guarding or PID checks |\n| Cross-platform terminal issues | Fallback to line-based prompts if TTY not detected |\n| User confusion | Clear, unambiguous option descriptions + short help alias |\n\n---\n\n## ✅ Actionable Implementation Checklist\n\n- [ ] Define exact detection signatures (files, dirs, version format)\n- [ ] Implement `detect_spine_init()` with clear return schema\n- [ ] Design CLI prompt interface (interactive + non-interactive fallbacks)\n- [ ] Map user selections to execution branches (full / config-preserve / skip)\n- [ ] Implement safe backup/restore logic for config preservation\n- [ ] Add `--skip`, `--reinit`, `--reinit-config` CLI flags\n- [ ] Write unit/integration tests for all detection & execution paths\n- [ ] Update CLI help text, README, and migration/docs for safeguard behavior\n- [ ] Add logging & audit trail for destructive operations\n- [ ] Review for pipeline/CI compatibility & document non-interactive usage\n\n---\n\n## 🔗 Dependencies & Prerequisites\n- CLI framework must support interactive prompts or flag parsing\n- Access to repo root & read/write permissions to `.spine/` directory\n- Existing init artifact structure must be known/versioned for reliable detection\n- Logging infrastructure for safe fallback & audit tracking\n\n---\n**Next Phase Recommendation:** `DESIGN`  \nProceed to architecture the detection module, prompt interface, and execution branching logic with sequence diagrams & component interfaces.'}}
- tasks_executed: 1
- total_tasks: 1
- errors: []
- error: None

### TECH_RESEARCH
- subphase: TECH_RESEARCH
- agent_role: sme
- status: success
- tasks: {'research_stack': {'status': 'success', 'result': '\n\n# Tech Research: `spine init` Safeguard Implementation\n\n## 1. Detection Architecture\nTo reliably identify an existing `spine` setup, implement a deterministic, multi-layer detection strategy:\n- **Primary Signature**: Presence of `.spine/` directory + `spine.config` (JSON/TOML)\n- **Secondary Markers**: \n  - `.spine.lock` or `.spine.state.json` (tracks init version, timestamp, flags)\n  - Project `.gitignore` entries namespaced to spine (e.g., `.spine/*`, `node_modules/.spine`)\n  - Presence of spine-generated source files or templates\n- **Tertiary Fallback**: Heuristic scan for spine-specific paths, modified CI configs, or injected environment variables\n- **Cross-Platform Notes**: Normalize paths via `path.resolve()`, respect case sensitivity on Windows/macOS, ignore symlink loops.\n\n## 2. State & Configuration Model\nSeparate runtime state from user-managed configuration to enable safe reinitialization:\n```json\n// .spine/spine.state.json\n{\n  "schema_version": 1,\n  "init_timestamp": "2024-06-15T10:00:00Z",\n  "init_version": "1.2.0",\n  "flags": {"verbose": true, "ci_optimized": false},\n  "config_snapshot_hash": "sha256:abc123...",\n  "backup_path": ".spine/backups/init-20240615T100000"\n}\n```\n- `config_snapshot_hash` enables diffing during preserve reinit\n- `backup_path` tracks pre-modification state for rollback\n- Schema versioning prevents migration failures on stale setups\n\n## 3. Safeguard Workflow Design\nPre-flight execution flow:\n1. **Scan** project root for detection signatures\n2. **Branch** on presence:\n   - `NOT_FOUND` → Proceed with standard init\n   - `FOUND` → Route to safeguard handler\n3. **Safeguard Handler**:\n   - Check for non-interactive flags (`--reset`, `--preserve`, `--skip`)\n   - If interactive: prompt user with 3 options + short descriptions\n   - If CI/automated: fail-safe to `--skip` or require explicit flag\n4. **Execution Path**:\n   - `Complete Reinit`: Backup current `.spine/` → Wipe state → Run full init\n   - `Preserve Config`: Load existing config → Merge with defaults → Update state → Run init\n   - `Do Nothing`: Exit cleanly with informational message\n5. **Post-Execution**: Log outcome, update state file, verify integrity\n\n## 4. Recommended Technology Stack\n| Component | Recommendation | Rationale |\n|-----------|----------------|-----------|\n| **CLI Framework** | `commander` + `inquirer` (Node.js) or `click` + `questionary` (Python) | Mature prompt handling, deterministic flag parsing, CI-safe modes |\n| **Config Parsing** | `jsonc`/`toml` with schema validation (`zod`/`pydantic`) | Strict typing, migration safety, comment preservation |\n| **File Operations** | Atomic writes (`fs.writeFileSync` + temp rename), safe mkdirp | Prevents corruption on interrupt, cross-OS consistent |\n| **State Management** | Single source-of-truth `.spine/state.json` with versioning | Enables diff, backup, rollback, and audit trails |\n| **Testing** | `fs-mock` + integration suites for init flow | Covers partial init, corrupted state, CI vs interactive modes |\n\n## 5. Implementation Roadmap\n1. **Define Detection Contract**: Finalize file/directory signatures & fallback heuristics\n2. **Build Pre-Flight Scanner**: Isolate detection logic into pure function (`detectExistingSpine(root)`)\n3. **Implement State Router**: Branch logic handling `--reset`/`--preserve`/`--skip` + interactive prompts\n4. **Add Backup/Rollback Layer**: Create timestamped backup dir, validate pre-destructive state\n5. **Support CI/CD Flags**: Ensure all interactive paths have non-interactive equivalents\n6. **Write Tests**: Unit (detection, state parsing), Integration (full flow, edge cases, interrupt handling)\n7. **Document Behavior**: Update CLI help, add `--help` examples, version bump in release notes\n\n## 6. Risks & Mitigations\n| Risk | Mitigation |\n|------|------------|\n| Corrupted/partial init state | Fallback to manual repair flow; validate state schema before branching |\n| Accidental config loss | Mandatory backup before `--reset`; auto-restore on failed init |\n| Conflicting tool markers | Use strict `.spine/` namespacing; document exclusion for other tools |\n| Multi-user/CI race conditions | File locking or state versioning; exit if state is locked/mismatched |\n| Prompt blocking in headless env | Default to `--skip` in non-TTY; require explicit flags for automation |\n\n## 7. Immediate Action Items\n- [ ] Lock detection signature spec (primary/secondary markers)\n- [ ] Draft `spine.state.json` schema & version migration path\n- [ ] Prototype pre-flight scanner + 3-way branch logic\n- [ ] Define CI-safe flag matrix (`--reset`, `--preserve`, `--skip`, `--force`)\n- [ ] Create test matrix: clean repo, partial init, corrupted state, CI headless, interactive TTY\n\n**Output Ready for Engineering Handoff**.|** This research provides a deterministic detection model, safe state management, and a production-ready safeguard workflow. Next step: implement prototype scanner + branch router in the `spine init` entry point.'}}
- tasks_executed: 1
- total_tasks: 1
- errors: []
- error: None

### RISK_ASSESSMENT
- subphase: RISK_ASSESSMENT
- agent_role: analyst
- status: success
- tasks: {'assess_risks': {'status': 'success', 'result': '\n\n# RISK ASSESSMENT REPORT: `spine init` Safeguard Implementation\n**Phase:** RISK_ASSESSMENT | **Task:** assess_risks | **Analyst Role:** Analyst\n\n## 1. Executive Summary\nThe requirement to add pre-flight detection and triage options (`full reinit`, `config-preserving reinit`, `abort`) to `spine init` introduces significant state-management, user-experience, and data-integrity risks. Primary concerns revolve around detection accuracy, destructive operation safety, non-interactive environment compatibility, and configuration schema migration. This assessment identifies risks, maps constraints, and provides actionable mitigations to be integrated into the design and implementation phases.\n\n---\n\n## 2. Risk & Constraint Matrix\n\n| ID | Risk Category | Risk Description | Likelihood | Impact | Mitigation / Constraint Handling | Residual Risk |\n|----|---------------|------------------|------------|--------|----------------------------------|---------------|\n| R01 | Technical | **False negative/positive detection**: Tool fails to detect existing spine setup or incorrectly flags a fresh repo. | Medium | High | Use deterministic markers (e.g., `.spine/config.yml`, CLI state DB, lock files). Implement version-aware detection logic. Add `--force-detect` for debugging. | Low |\n| R02 | Data Integrity | **Data loss during `full reinit`**: Irreversible deletion of user scripts, custom hooks, or generated artifacts. | Medium | Critical | Pre-execution dry-run + backup snapshot. Require explicit `--confirm` flag. Default option to `abort` unless `--yes` is passed. | Low |\n| R03 | Configuration | **Schema mismatch during `config-preserving reinit`**: New spine version introduces breaking config fields; merge logic drops or overwrites values. | High | High | Implement config versioning + schema validation. Generate diff/preview before applying. Provide `--skip-merge` fallback. | Medium |\n| R04 | Operational | **Non-interactive environment failure**: CI/CD, headless scripts, or batch jobs hang on CLI prompt or error on TTY detection. | High | Medium | Auto-detect TTY. Supply `--yes`, `--no`, `--dry-run` flags. Log options clearly when interactive mode is disabled. | Low |\n| R05 | Security | **Credential/secret exposure**: Reinit process touches vault, API keys, or env files; logs or prompts leak sensitive data. | Medium | High | Never prompt for secrets. Mask outputs. Log only hashed refs or config keys. Enforce read-only mode during detection phase. | Low |\n| R06 | UX/Adoption | **Choice paralysis or mis-selection**: Users pick `full reinit` due to unclear descriptions, causing rework. | Medium | Medium | Provide inline help, severity labels (`⚠️ Destructive`, `✅ Safe`), and `--preview` mode. Default to `abort` with clear next steps. | Low |\n| R07 | Performance | **Repo scanning overhead**: Large monorepos or repos with heavy ignores cause detection to timeout or hang. | Low | Medium | Bound scan depth, skip `.git`, `node_modules`, build dirs, and vendor folders. Add `--timeout` and progress indicators. | Low |\n\n---\n\n## 3. Key Constraints & Dependencies\n\n| Constraint | Description | Impact on Implementation |\n|------------|-------------|--------------------------|\n| **Backward Compatibility** | Existing repos may run legacy `spine` CLI versions or non-standard directory structures. | Detection logic must be version-agnostic and fallback gracefully. Provide migration path docs. |\n| **Platform Parity** | Cross-OS differences (Windows paths, case sensitivity, shell escaping). | Abstract file/path operations behind a platform-agnostic layer. Test on Win/macOS/Linux. |\n| **CI/CD Integration** | Pipelines expect deterministic, non-blocking CLI behavior. | CLI must support silent/flag-driven execution. No stdin prompts without explicit override. |\n| **State Isolation** | Global vs local state confusion across multiple repos on the same machine. | Enforce per-repo isolation. Avoid mutating global CLI state during `init`. |\n| **Audit & Compliance** | Organizations may require traceability of destructive operations. | Log all reinit actions with timestamp, user, repo path, and selected option. Provide `--audit` flag. |\n\n---\n\n## 4. Actionable Mitigations & Implementation Guardrails\n\n1. **Detection Layer**\n   - Implement a deterministic check sequence: config file → CLI state DB → known directory markers → version file.\n   - Return structured detection payload: `{ detected: boolean, version: string, configPath: string, hasCustomOverrides: boolean }`.\n\n2. **Option Execution Safeguards**\n   - **Full Reinit**: Pre-check backup, generate timestamped `.spine.backup/`, require `--confirm` + `--yes`.\n   - **Config-Preserving Reinit**: Run schema diff, output `--preview` changes, apply only if user approves or flag is set.\n   - **Do Nothing**: Exit cleanly with guidance: `"Spine is already initialized. Use \'spine status\' or \'spine migrate\' for next steps."`\n\n3. **CLI Contract & Flag Design**\n   ```bash\n   spine init [flags]\n   --yes, -y        # Auto-confirm destructive paths\n   --no, -n         # Abort on detection\n   --preview        # Show changes without applying\n   --skip-merge     # Skip config preservation step\n   --dry-run        # Simulate detection & options without I/O\n   --timeout <ms>   # Max time for repo scanning\n   ```\n\n4. **Testing Requirements**\n   - Unit tests for detection logic across schema versions.\n   - Integration tests simulating CI/CD, large repos, and permission-restricted dirs.\n   - Chaos testing: corrupt config, missing files, overlapping state files.\n\n---\n\n## 5. Recommended Next Steps\n\n| Action | Owner | Timeline | Success Metric |\n|--------|-------|----------|----------------|\n| Draft detection algorithm & marker schema | Backend/CLI Dev | Sprint 1 | Deterministic detection in 100% of test repos |\n| Implement pre-flight triage UI/CLI flow | Frontend/UX Dev | Sprint 1 | Non-interactive fallback works in headless envs |\n| Build config merge/diff engine | Backend Dev | Sprint 2 | Zero data loss in schema migration tests |\n| Add logging, audit trail, and backup logic | Security/DevOps | Sprint 2 | All destructive ops logged; backups recoverable |\n| Run cross-platform & CI integration validation | QA/DevOps | Sprint 3 | 0 blocking defects; <2s detection on 500MB repos |\n\n---\n\n## 6. Analyst Sign-Off Criteria\n- [ ] Detection logic handles legacy, current, and future schema versions without false positives.\n- [ ] All three options execute safely with explicit user confirmation or CLI flags.\n- [ ] Non-interactive environments bypass prompts without hanging or crashing.\n- [ ] Config preservation includes diff preview and rollback capability.\n- [ ] Security review confirms no secret leakage or unsafe file permissions.\n\n**Next Phase Dependency:** `DESIGN_PHASE` (CLI contract finalization, state machine flow, config merge architecture)  \n**Risk Owner:** Technical Lead / Product Owner  \n**Review Cycle:** Reassess after prototype demo & CI integration test results.'}}
- tasks_executed: 1
- total_tasks: 1
- errors: []
- error: None

### SYNTHESIZE
- subphase: SYNTHESIZE
- agent_role: planner
- status: success
- tasks: {'draft_plan': {'status': 'success', 'result': "\n\n# Execution Plan: `spine init` Safeguard & Reinitialization Options\n\n## 🎯 Objective\nIntegrate a detection & safeguard mechanism into the `spine init` workflow to identify existing Spine setups, prompt the user with three explicit options, and execute the chosen path safely without data loss or state corruption.\n\n---\n\n## 📋 Phase Breakdown\n\n### Phase 1: State Detection & Analysis\n**Goal:** Accurately determine if Spine is already initialized in the target repository.\n- [ ] Define canonical Spine initialization markers (e.g., `.spine/` directory, `spine-config.yaml`, `package.json` fields, lockfiles)\n- [ ] Implement `detectSpineState(repoPath) → { initialized: boolean, state: 'full'|'partial'|'corrupted', configPath?: string }`\n- [ ] Add graceful fallbacks for missing/permission-denied markers\n- [ ] Log detection results with debug/trace flags\n- **Deliverable:** Detection utility + unit tests covering fresh, initialized, partial, and corrupted repos\n- **Validation:** Returns correct state across 5+ mock repo scenarios; handles OS/file system edge cases\n\n### Phase 2: Interactive Safeguard Prompt\n**Goal:** Present clear, unambiguous options to the user when an existing setup is detected.\n- [ ] Design CLI prompt interface (interactive menu or inline prompt)\n- [ ] Map options:\n  1. `full-reset` → Wipe existing Spine state & reinitialize from scratch\n  2. `preserve-config` → Backup current config, reset state, reinitialize & restore settings\n  3. `abort` → Exit gracefully, print current state summary\n- [ ] Add confirmation step before destructive actions (backup + reset)\n- [ ] Handle invalid/cancel input (default to `abort`, retry prompt)\n- [ ] Ensure ANSI-safe output & compatibility with non-TTY environments (fallback to CLI flags: `--reset`, `--preserve-config`, `--skip`)\n- **Deliverable:** Prompt module + UX flow diagram + flag overrides\n- **Validation:** Users successfully navigate all paths; non-interactive CLI flags work as documented\n\n### Phase 3: Execution Path Implementation\n**Goal:** Safely execute the chosen initialization path with atomicity and rollback safety.\n- [ ] **Path A: `full-reset`**\n  - Remove `.spine/` directory & related artifacts\n  - Clear cache/state\n  - Proceed with standard `spine init` flow\n- [ ] **Path B: `preserve-config`**\n  - Serialize & backup config (`spine-config.yaml` or equivalent) to temp location\n  - Remove Spine artifacts (same as full-reset)\n  - Run `spine init`\n  - Merge/restore backup config, resolve conflicts if any\n  - Validate restored config integrity\n- [ ] **Path C: `abort`**\n  - Print state summary\n  - Exit with code `0`\n  - No filesystem modifications\n- [ ] Ensure idempotency: re-running `spine init` on a freshly initialized repo should skip safeguard if no conflicts exist\n- **Deliverable:** Path executors + config backup/restore module + integration tests\n- **Validation:** Filesystem state matches expected outcome; config preserved correctly; zero unintended side effects\n\n### Phase 4: Integration & Error Handling\n**Goal:** Seamlessly embed safeguard into `spine init` workflow with robust error recovery.\n- [ ] Hook detection & prompt into entry point (`spine init`) before any write operations\n- [ ] Add transactional wrapper: checkpoint filesystem state before reset, rollback on failure\n- [ ] Handle edge cases:\n  - Concurrent initialization attempts\n  - Read-only directories / permission issues\n  - Network-dependent steps (if init pulls templates/assets)\n  - Corrupted backup files\n- [ ] Add structured logging (debug, warn, error levels) for auditability\n- [ ] Expose `--force` flag to bypass prompt in CI/CD pipelines\n- **Deliverable:** Integrated workflow module + error handlers + CI override flags\n- **Validation:** Passes integration suite; handles 100% of tested failure modes without leaving repo in broken state\n\n### Phase 5: Testing & Validation\n**Goal:** Verify correctness, UX, and safety across environments.\n- [ ] Unit tests: detection logic, config serialization, prompt routing\n- [ ] Integration tests: fresh repo, initialized repo, partial init, corrupted markers\n- [ ] E2E tests: simulate real `spine init` runs with all 3 options + `--force`\n- [ ] UX validation: non-TTY fallback, color/ANSI compliance, prompt timeout handling\n- [ ] Performance check: safeguard should add <500ms overhead to init flow\n- **Deliverable:** Test suite + coverage report + E2E validation logs\n- **Validation:** ≥90% line coverage; zero flaky tests; all paths pass E2E\n\n### Phase 6: Documentation & Release\n**Goal:** Communicate changes, guide users, and ship safely.\n- [ ] Update CLI `--help` & man pages\n- [ ] Add changelog entry (breaking/feature note, flag docs, UX changes)\n- [ ] Write migration/usage guide for existing users\n- [ ] Prepare release notes & rollback plan\n- [ ] Deploy to internal/staging environment for QA sign-off\n- **Deliverable:** Docs PR + changelog + release package\n- **Validation:** Docs reviewed by tech writers; users report zero confusion on new flow\n\n---\n\n## ⚠️ Risk Mitigation\n| Risk | Mitigation |\n|------|------------|\n| Accidental data loss during reset | Mandatory confirmation prompt; config backup checksum verification |\n| Non-interactive CI breakage | `--force`/`--skip-safeguard` flags; fail-open only when explicitly flagged |\n| Partial/corrupted state misdetected | Fallback to manual override; detailed diagnostic output |\n| UX friction in terminals | ANSI-safe menu; clear descriptions; escape/cancel support |\n\n---\n\n## ✅ Success Criteria\n1. `spine init` correctly detects existing setups in 100% of test cases\n2. All three options execute without filesystem corruption or config loss\n3. Non-interactive pipelines can bypass safeguard safely via CLI flags\n4. Zero regressions in standard (fresh) init flow\n5. Documentation & CLI help accurately reflect new behavior\n\n---\n\n## 📅 Next Steps & Ownership\n- [ ] Assign implementation lead for Detection & Prompt modules\n- [ ] Set up mock repo fixtures for testing\n- [ ] Review UX copy & CLI flag syntax with product/UX team\n- [ ] Draft PR with phased commits: `detection` → `prompt` → `execution paths` → `integration` → `tests/docs`\n\n**Ready for implementation.** Let me know if you need stack-specific scaffolding (e.g., Node/Python/Rust CLI frameworks, config schema, or prompt library recommendations)."}, 'critic_review': {'status': 'success', 'result': '\n\n# 📋 CRITIC GATE REVIEW: `spine init` Safeguards\n**Agent Role:** Planner | **SubPhase:** SYNTHESIZE | **Task:** critic_review  \n**Requirement:** Add safeguards so if `spine init` is run in a repo that already has spine setup it will detect this and offer the options to completely reinitialize spine, reinitialise but keep configuration settings, do nothing.\n\n---\n\n## 🔍 Review Summary\n**Status:** `CONDITIONAL PASS`  \n**Verdict:** The requirement correctly identifies a common CLI friction point and proposes a user-centric mitigation strategy. However, it lacks implementation specifics, safety guarantees, and state-resolution logic required for safe execution. Refinement is needed before implementation.\n\n---\n\n## ✅ Strengths\n1. **Clear User Intent:** Explicitly defines three distinct paths for the user, reducing blind overwrites.\n2. **Idempotency Awareness:** Acknowledges that `init` should not assume a clean state.\n3. **Configuration Preservation:** Recognizes the need to retain user-defined settings during reinit.\n\n---\n\n## ⚠️ Critical Gaps & Ambiguities\n| Area | Issue | Impact |\n|------|-------|--------|\n| **Detection Heuristic** | No definition of how "spine setup" is identified. | Risk of false positives/negatives or missing partial setups. |\n| **CLI Interaction Model** | No specification of prompt format, interactivity, or fallback behavior. | Poor UX, potential scriptability issues, or accidental keypresses causing unwanted actions. |\n| **Scope of "Configuration"** vs "Generated Assets** | Unclear what constitutes config vs. auto-generated files/templates. | Risk of overwriting custom code or failing to preserve actual configs. |\n| **Dirty State Handling** | No mention of uncommitted changes, staged files, or custom modifications. | Silent data loss or conflict resolution failures. |\n| **Safety & Rollback** | No dry-run, confirmation step, or restore mechanism defined. | High risk of irreversible damage in production repos. |\n\n---\n\n## 🛡️ Risk Assessment\n| Risk | Likelihood | Severity | Mitigation |\n|------|------------|----------|------------|\n| Configuration loss | Medium | High | Explicit mapping of config vs. generated files; backup before overwrite |\n| Unintended overwrite | Low-Medium | High | Dry-run + explicit `y/N` confirmation + interactive menu |\n| Inconsistent detection | Medium | Medium | Define a deterministic marker file/dir structure |\n| UX friction in CI/CD | Low | Medium | Support `--force`/`--non-interactive` flags for automation |\n\n---\n\n## 🛠️ Actionable Recommendations\n1. **Define Detection Logic**\n   - Use a deterministic marker (e.g., `.spine/metadata.json` or `.spine/.initialized` flag).\n   - Fallback: scan for `.spine/` directory + existence of core config/templates.\n   - Return exit code `1` with clear message if detection fails ambiguously.\n\n2. **Specify CLI Interaction Pattern**\n   ```bash\n   ❯ spine init\n   ⚠️  A Spine setup is already detected in this repository.\n   \n   1️⃣  Completely reinitialize (backup existing setup to .spine/.backup/)\n   2️⃣  Reinitialize but preserve configuration settings\n   3️⃣  Do nothing & exit\n   \n   👉 Choose (1-3): _\n   ```\n   - Require explicit numeric input or `y/n` for automation-safe paths.\n   - Add `--dry-run` and `--yes` flags for CI/CD compatibility.\n\n3. **Clarify "Configuration" Scope**\n   - Define explicitly: e.g., `.spine/config.yaml`, `.spine/env.local`, custom templates in `.spine/custom/`.\n   - Document what gets overwritten vs. preserved during each option.\n\n4. **Pre-Check Dirty State**\n   - If `git status` shows uncommitted changes in tracked Spine files, warn:\n     `⚠️  Uncommitted changes detected. Proceeding may overwrite local modifications.`\n   - Require explicit `--force` or manual confirmation to continue.\n\n5. **Implement Safety Net**\n   - Auto-create `.spine/.backup/{timestamp}/` before any destructive path.\n   - Add post-operation validation (checksum or marker check) to confirm success.\n   - Provide `spine init --restore` flag to recover from accidental overwrite.\n\n---\n\n## 📐 Proposed State & Flow Architecture\n```\n[User runs `spine init`]\n        ↓\n[Check for .spine/metadata.json or equivalent marker]\n        ↓\n[If absent → proceed with standard init]\n        ↓\n[If present → run pre-flight checks (git dirty? partial init?)]\n        ↓\n[Present interactive menu with clear descriptions]\n        ↓\n[Map choice to execution path:\n  1 → Full reset + backup + reinit\n  2 → Preserve config scope + reinit generated assets\n  3 → Exit 0 with info message]\n        ↓\n[Validate post-state & log action taken]\n```\n\n---\n\n## � Gate Readiness Checklist\n- [ ] Detection heuristic defined & tested\n- [ ] CLI prompt specification finalized (interactivity, flags, exit codes)\n- [ ] Config vs generated asset boundary documented\n- [ ] Dirty-state & conflict handling specified\n- [ ] Backup/rollback mechanism implemented in spec\n- [ ] CI/CD non-interactive fallback documented (`--yes`, `--dry-run`)\n- [ ] Unit/e2e test scenarios outlined for all 3 paths\n\n---\n\n**Next Step for Planning:**  \nRefine the implementation specification using the recommendations above, then route to `IMPLEMENTATION` phase with updated task breakdown. Provide detection schema, prompt specification, and backup/restore logic as explicit sub-tasks.\n\n*Review completed by Planner (SYNTHESIZE) | Output ready for gate approval & downstream planning.*'}}
- tasks_executed: 2
- total_tasks: 2
- errors: []
- error: None

---
*Generated at 2026-05-10T05:18:46.122856+00:00*