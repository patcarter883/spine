spine:
  checkpoint_path: .spine/spine.db
providers:
  agent:
    - name: default
      type: opencode
      config:
        mode: acp
        model: openrouter/minimax/minimax-m2.7
        auto_approve: true
