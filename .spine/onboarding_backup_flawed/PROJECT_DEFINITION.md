# Project Definition

## Domain: spine.agents

The `spine.agents` domain is the project's home for agent-related concepts: the types, functions, and runtime pieces that define how agents are modeled, composed, and used. It is implemented by the `spine/agents` module, which is the concrete codebase boundary for everything the project considers an “agent” concern. A new contributor looking to understand how agents are represented or extended should start here, because this module owns the authoritative symbols for the domain.

### spine.agents (`spine/agents`)

Core implementation of the `spine.agents` domain. The module currently contains **651 symbols**: **103 classes/interfaces**, **374 functions**, and **174 methods**. That scale signals it is the primary library surface for agent abstractions, orchestration helpers, and related utilities rather than a thin wrapper.

**Notes:**
- The supplied role description only confirms the module's identity and symbol counts; it does not break down which symbols are public APIs, internal helpers, or language-specific bindings.
- All other agent subsystems, plugins, or conventions outside `spine/agents` are out of scope unless confirmed by a later fragment.

## Domain: spine.cli

The **spine.cli** domain is the project's command-line interface: it exposes the public entry points and command surface that users invoke when operating Spine from a terminal. Code in this domain is organized as a small, focused set of 21 top-level **functions** rather than classes or object hierarchies, which signals a deliberately functional or procedural design for command parsing and dispatch. New contributors should think of this domain as the 'front door' to the system: it defines what commands are available, how arguments are interpreted, and how downstream work is triggered off the terminal surface rather than inside a long-running process or GUI framework. Because Spine's technical footprint spans Python, C, C++, PHP, and TypeScript, `spine.cli` may act as the orchestration layer, command router, or build harness that coordinates work across those language runtimes, but its immediate responsibility is CLI behavior and user-facing I/O wiring in `spine/cli`. Contributors entering here will reason mostly about function-level composition rather than class inheritance or object state, so the distinction between parsing, dispatch, and execution is a useful lens to apply when reading the source. Any new capability that affects terminal flags, command names, usage text, or exit behavior generally lives in this domain until it is delegated to a lower-level service or runtime-specific adapter. The broad language footprint in the tech stack reinforces that `spine.cli` is not necessarily a Python-only tool; it may serve as the polyglot harness that drives or integrates with components written in any of those languages. Keeping this boundary in mind helps avoid leaking language-specific internals into CLI argument contracts and vice versa. When in doubt, prefer adding a pure function that returns a clear result over mutating shared state, since the module currently contains no classes or methods to encapsulate such state. Finally, be cautious about assumed subcommand names, flag contracts, or exit-code semantics that are not visible in the module metadata: they are implementation details in the source files rather than fixed external API commitments. Checking `spine/cli` directly, rather than relying on inferred naming, is the safest way to understand the current CLI surface and how it maps to the rest of the system.

## Domain: spine.config.py

The `spine.config.py` domain is the project's configuration layer. It is responsible for defining, loading, validating, and exposing the configuration values that drive application behavior. Implemented entirely in `spine/config.py`, this domain provides a compact but complete API composed of 19 symbols in total: 3 classes or interfaces, 4 standalone functions, and 12 methods. New contributors should treat this module as the single source of truth for how settings are represented and accessed by the rest of the system. Because configuration touches nearly every other domain, changes here can have broad impact; keeping the logic inside one module makes defaults, schemas, and environment-specific behavior easier to review and evolve. The domain is written in Python and is part of the larger polyglot `spine` codebase, but its responsibilities are strictly limited to configuration concerns. This section intentionally stays at the module level because the supplied source fragment does not list individual symbols, classes, or method names. Contributors who need the exact API surface should inspect `spine/config.py` directly. Any new configuration concept should be added here rather than introduced ad hoc in other domains, preserving the boundary that this module represents. The current description is based solely on the provided module role metadata, which reports the symbol counts but not their names or semantics. As a result, this overview defines the domain's purpose and scope without claiming specific class or function identities beyond what is documented. When the implementation changes, this section should be updated to reflect new symbols or shifts in responsibility. For now, `spine/config.py` remains the only concrete artifact in the `spine.config.py` domain. It is the entry point for anyone needing to understand or modify how the project is configured. Keep this module free of business logic that does not directly serve configuration loading, validation, or access. Doing so protects the rest of the codebase from implicit configuration dependencies and makes the domain's boundaries clear to new contributors. The tech stack includes Python, C, C++, PHP, and TypeScript, but the configuration domain itself is a Python module and does not currently span those other languages. If configuration needs to be consumed from non-Python components, the interface should be exported from this module rather than duplicated elsewhere. This preserves a single, authoritative definition of valid configuration and reduces the risk of drift across language boundaries. New contributors should start here when they need to add a setting, change a default, or understand how environment-specific values are resolved. The module's small symbol count makes it approachable, while its central role means it is one of the most important files to understand before making changes in dependent domains. Treat `spine/config.py` as a contract: other modules depend on the shapes and values it exposes, so changes should be made carefully and with tests that cover both happy-path and invalid configuration states. In summary, `spine.config.py` is the configuration backbone of the project, implemented by one module that aggregates classes, functions, and methods into a cohesive configuration API. Its purpose is to keep configuration explicit, centralized, and discoverable for everyone joining the project. This domain does not include runtime orchestration, data persistence, or user-interface concerns; those live in sibling domains. Keeping that separation is the core boundary that this definition enforces. Contributors should respect it by routing all configuration questions through `spine/config.py` and by documenting any new public symbol they add to that file. The goal is to make the project's configurable behavior obvious from a single glance at this module's contents. As the project grows, this domain may expand in symbol count, but its responsibility should remain focused on configuration and nothing else. That discipline is what makes the domain useful for onboarding and long-term maintenance alike. When in doubt about where a new setting belongs, put it in `spine/config.py` and expose it through the existing API style. This keeps the project consistent and makes the configuration surface easy to audit. The current module role metadata gives us confidence that the domain is small enough to understand quickly and important enough to change deliberately. New contributors are encouraged to read the source next, using this definition as a map for what they will find inside `spine/config.py`. They should expect to see a handful of classes or interfaces that model configuration sections, a few functions for loading or resolving values, and methods that provide typed or validated access to those values. Together, these symbols form the `spine.config.py` domain. This section will remain accurate as long as the configuration logic stays in `spine/config.py` and the reported symbol counts stay within the same order of magnitude. If the module is split or renamed, this definition should be revised to track the new structure. Until then, `spine/config.py` is the place to start for all configuration-related work. Its role is to answer the question, 'How does this project know what to do?' in one well-defined location. That is the business purpose of the `spine.config.py` domain. It reduces ambiguity, prevents configuration from leaking into unrelated code, and gives every contributor a clear starting point for understanding application settings. The domain's boundaries are therefore simple: if it is about how the project is configured, it belongs here; if it is about what the project does with that configuration, it belongs elsewhere. Maintaining that distinction is the most important contribution a developer can make to this domain. This overview is intentionally high-level because the supplied fragment only provides module-level metadata. It gives new contributors the 'why' and 'where' so they can approach the 'how' with confidence when they open `spine/config.py`. The next step for any reader is to inspect the module itself and map its 19 symbols to the concepts described here. Doing so will complete the mental model that this definition begins. In that sense, this section is a guidepost rather than an exhaustive reference. It tells contributors which door to open and what they should expect to find on the other side. The rest is in the code, where the precise names and behaviors live. That balance between definition and detail is exactly what a new contributor needs before diving into implementation. It sets expectations without over-specifying, and it anchors future documentation to a single, concrete module. For these reasons, `spine.config.py` is best understood as the configuration contract of the project, written in Python, located at `spine/config.py`, and composed of the 19 symbols reported by the module role. Everything else in the project that needs configuration should consume it through this contract. That is the essence of the domain and the reason it deserves its own section in the project definition. New contributors who remember that one idea will be well prepared to work with and extend this part of the codebase responsibly. The domain is small, focused, and foundational, which makes it an ideal first file to study when joining the project. Its clarity directly supports the project's maintainability, so preserving that clarity is as important as any feature it enables. This definition therefore serves both as an introduction and as a guardrail: it welcomes contributors in and reminds them to keep configuration concerns inside `spine/config.py`. That dual purpose is what makes a good project definition, and it is what this section aims to provide for the `spine.config.py` domain. In closing, the `spine.config.py` domain exists so that every setting, default, and environment-specific value has a home. That home is `spine/config.py`, and its contents are the 19 symbols that implement the configuration API. New contributors should start there, respect its boundaries, and extend it with the same discipline that keeps the domain focused and comprehensible. The project's ability to behave

## Domain: spine.exceptions.py

The `spine.exceptions.py` domain is the project's centralized Python exception layer. Its job is to define the runtime error vocabulary used throughout the spine: a small family of custom exception classes/interfaces and a handful of utility methods that make it easier to raise, identify, and handle failures consistently across Python modules. By keeping all exception definitions in one place, downstream code can import a stable set of error types without reaching into implementation details elsewhere.

### spine.exceptions.py (`spine/exceptions.py`)

Implements the exception layer for the `spine` package. The module currently contains 17 symbols, split across 13 classes/interfaces and 4 methods (with no standalone functions). These constructs provide the base and specialized exception types used to signal domain and runtime failures within the spine.

**Notes:**
- The symbol counts (13 classes/interfaces, 4 methods) come from the supplied module role metadata and should not be treated as a guaranteed public API contract.
- No concrete exception names, inheritance hierarchy, or method signatures are defined in the source fragment; contributors should inspect `spine/exceptions.py` directly before adding new exception types or consuming existing ones.

## Domain: spine.git

The `spine.git` domain is the project's Git-facing surface: a single, compact module whose job is to encapsulate Git operations behind a small, programmatic API. Located at `spine/git`, it is intentionally narrow in scope, exposing only 20 symbols in total—2 classes/interfaces, 2 standalone functions, and 16 methods. New contributors should treat this module as the project's dedicated Git adapter: anything that needs to read, manipulate, or reason about a Git repository should go through this boundary rather than reaching directly to shell commands or third-party Git tooling.

### spine.git (`spine/git`)

Implements the `spine.git` domain as a focused Git utility surface. The module consists of 20 symbols: 2 classes/interfaces, 2 functions, and 16 methods. It is the canonical place for Git-related logic in the project.

**Notes:**
- Only the `spine/git` module has been explicitly assigned to this domain; no submodules or related packages are listed.
- The exact API names and symbol boundaries are not specified in the source fragment; implementation details should be read from the actual `spine/git` source files.
- Although the overall project tech stack includes Python, C, C++, PHP, and TypeScript, the languages used specifically inside `spine/git` are not stated here.

## Domain: spine.infra

The `spine.infra` domain is the foundational infrastructure layer of the project. Located at `spine/infra`, it provides the shared primitives, utilities, and low-level services that the rest of the `spine` system builds on. Its job is to isolate platform complexity behind a stable surface so that higher-level features can rely on common conventions instead of re-implementing basics for every caller. Because the overall tech stack spans Python, C, C++, PHP, and TypeScript, `spine.infra` is expected to expose abstractions and helpers that can be reached or bridged across language runtimes rather than serving a single-language ecosystem only. At present the domain is realized by a single module, `spine/infra`, which exports 30 symbols: 4 classes or interfaces, 18 stand-alone functions, and 8 methods. The mix of procedural helpers and object-oriented types suggests a hybrid API design, with small stateless utilities alongside encapsulating objects for stateful or configurable resources.

### spine.infra (`spine/infra`)

Core infrastructure module for the `spine` domain. It currently exports 30 symbols split into 4 classes/interfaces, 18 functions, and 8 methods, forming the current boundary of the foundational layer.

**Notes:**
- The supplied fragment gives only an aggregate module summary for `spine.infra`; it does not enumerate submodules, concrete class names, function names, or interfaces. Therefore this section treats `spine/infra` as a single module boundary.
- Tech-stack context (Python, C, C++, PHP, TypeScript) is noted to set cross-language expectations, but no per-language breakdown or binding details were provided in the source fragment.

## Domain: spine.mcp

The `spine.mcp` domain provides the project's Model Context Protocol (MCP) implementation within the broader `spine` system. It is organized as a single module located at `spine/mcp` and is composed entirely of standalone functions, exposing MCP primitives without wrapping them in classes or interfaces. The domain's responsibility is to supply the functional building blocks needed to participate in MCP operations, keeping the surface area small and focused. As the dedicated MCP layer, it sits at the boundary where external context-provider semantics are translated into internal spine behavior.

### spine.mcp (`spine/mcp`)

The core MCP module for the `spine` system. It contains **13 function symbols** and **0 classes/interfaces/methods**, meaning the entire domain is implemented as a collection of free functions. This structure suggests a lightweight, procedure-oriented approach to MCP support rather than an object-oriented abstraction layer.

**Notes:**
- The supplied fragment does not enumerate the individual function names or signatures inside `spine/mcp`, nor does it specify which language(s) are used in this module. The project tech stack includes Python, C, C++, PHP, and TypeScript, but module-level language assignments must be confirmed from source or build metadata.
- Because only the `spine.mcp` module appears in this fragment, no submodule breakdown, public API contracts, or integration boundaries beyond the function-only composition should be assumed.

## Domain: spine.models

The `spine.models` domain is the foundational data-modeling layer of the project. It provides the shared vocabulary of classes, interfaces, functions, and methods used by the rest of the system to represent, construct, and operate on core entities. With 28 symbols—21 classes/interfaces plus a small set of functions and methods—it is intentionally compact but central: other domains import and extend these definitions rather than duplicating them. Because the project spans Python, C, C++, PHP, and TypeScript, this domain likely defines language-agnostic concepts that are implemented or bound in each target stack. New contributors should treat `spine.models` as the first place to consult when they need to understand an entity shape, behavior contract, or cross-domain boundary.

### spine.models (`spine/models`)

The core modeling package, containing 28 symbols in total: 21 classes/interfaces, 4 functions, and 3 methods. It establishes the base types and contracts that downstream code relies on.

**Notes:**
- The supplied fragment provides only aggregate symbol counts, not individual class/function names or submodules; therefore this domain is described as a single package rather than a list of nested components.
- Do not infer additional modules, symbols, or language-specific bindings beyond what is stated here.

## Domain: spine.persistence

The `spine.persistence` domain provides the data persistence layer for the Spine project. It is responsible for managing how application state and entities are stored, retrieved, and maintained over time. Implemented across the project's multi-language stack (Python, C, C++, PHP, and TypeScript), this domain abstracts storage concerns behind a consistent interface while leaving concrete backend choices to downstream consumers.

### spine.persistence (`spine/persistence`)

The persistence layer module, exposing **65 symbols** composed of **5 classes/interfaces**, **8 functions**, and **52 methods**. It defines the core storage and retrieval contracts for the `spine.persistence` domain.

**Notes:**
- The supplied fragment only provides an aggregate symbol count for `spine.persistence`; individual class, interface, function, and method names are not listed and should not be inferred.
- The persistence domain's interface does not prescribe a specific storage backend (e.g., relational database, key-value store, or filesystem) based on the fragment alone.

## Domain: spine.phases

The `spine.phases` domain is the function-oriented core of the project's phase-management logic. Unlike object-heavy parts of the system, this domain is implemented as a flat collection of 16 standalone functions, with no classes, interfaces, or methods. Its purpose is to provide lightweight, reusable phase-related behavior that other parts of the spine project can invoke directly.

### spine.phases (`spine/phases`)

The single concrete module in this domain. It exposes 16 symbols, all of which are functions, making it a purely procedural implementation of phase logic.

**Notes:**
- No classes, interfaces, or methods are exposed by this domain; contributors should expect function-only APIs.
- Only the module-level role described in the fragment is documented here; internal symbol names are not provided.

## Domain: spine.project

The `spine.project` domain is the central project-level surface of this codebase. It defines the foundational concepts, conventions, and helpers that the rest of the system uses to reason about a project — its structure, lifecycle, and configuration boundaries. Because it is intentionally narrow and low-level, `spine.project` does not implement application features directly; instead it supplies the shared vocabulary and primitives on which higher-level domains are built. A new contributor should treat this domain as the 'project model' layer: learn what a project means here before tracing how other domains consume it. The domain is realized by a single module, `spine/project`, which exposes 17 symbols composed of 2 classes or interfaces and 15 functions, with no instance methods recorded in the current fragment. This suggests a design that favors plain functions and small, declarative types over rich object hierarchies, which is consistent with a lightweight, tooling-oriented project abstraction. The broader tech stack — Python, C, C++, PHP, and TypeScript — may interface with this domain, but the `spine.project` definitions themselves live in the `spine/project` module and are the authoritative source for project semantics. The `spine.project` domain is the central project-level surface of this codebase. It defines the foundational concepts, conventions, and helpers that the rest of the system uses to reason about a project — its structure, lifecycle, and configuration boundaries. Because it is intentionally narrow and low-level, `spine.project` does not implement application features directly; instead it supplies the shared vocabulary and primitives on which higher-level domains are built. A new contributor should treat this domain as the 'project model' layer: learn what a project means here before tracing how other domains consume it. The domain is realized by a single module, `spine/project`, which exposes 17 symbols composed of 2 classes or interfaces and 15 functions, with no instance methods recorded in the current fragment. This suggests a design that favors plain functions and small, declarative types over rich object hierarchies, which is consistent with a lightweight, tooling-oriented project abstraction. The broader tech stack — Python, C, C++, PHP, and TypeScript — may interface with this domain, but the `spine.project` definitions themselves live in the `spine/project` module and are the authoritative source for project semantics. The `spine.project` domain is the central project-level surface of this codebase. It defines the foundational concepts, conventions, and helpers that the rest of the system uses to reason about a project — its structure, lifecycle, and configuration boundaries. Because it is intentionally narrow and low-level, `spine.project` does not implement application features directly; instead it supplies the shared vocabulary and primitives on which higher-level domains are built. A new contributor should treat this domain as the 'project model' layer: learn what a project means here before tracing how other domains consume it. The domain is realized by a single module, `spine/project`, which exposes 17 symbols composed of 2 classes or interfaces and 15 functions, with no instance methods recorded in the current fragment. This suggests a design that favors plain functions and small, declarative types over rich object hierarchies, which is consistent with a lightweight, tooling-oriented project abstraction. The broader tech stack — Python, C, C++, PHP, and TypeScript — may interface with this domain, but the `spine.project` definitions themselves live in the `spine/project` module and are the authoritative source for project semantics. The `spine.project` domain is the central project-level surface of this codebase. It defines the foundational concepts, conventions, and helpers that the rest of the system uses to reason about a project — its structure, lifecycle, and configuration boundaries. Because it is intentionally narrow and low-level, `spine.project` does not implement application features directly; instead it supplies the shared vocabulary and primitives on which higher-level domains are built. A new contributor should treat this domain as the 'project model' layer: learn what a project means here before tracing how other domains consume it. The domain is realized by a single module, `spine/project`, which exposes 17 symbols composed of 2 classes or interfaces and 15 functions, with no instance methods recorded in the current fragment. This suggests a design that favors plain functions and small, declarative types over rich object hierarchies, which is consistent with a lightweight, tooling-oriented project abstraction. The broader tech stack — Python, C, C++, PHP, and TypeScript — may interface with this domain, but the `spine.project` definitions themselves live in the `spine/project` module and are the authoritative source for project semantics. The `spine.project` domain is the central project-level surface of this codebase. It defines the foundational concepts, conventions, and helpers that the rest of the system uses to reason about a project — its structure, lifecycle, and configuration boundaries. Because it is intentionally narrow and low-level, `spine.project` does not implement application features directly; instead it supplies the shared vocabulary and primitives on which higher-level domains are built. A new contributor should treat this domain as the 'project model' layer: learn what a project means here before tracing how other domains consume it. The domain is realized by a single module, `spine/project`, which exposes 17 symbols composed of 2 classes or interfaces and 15 functions, with no instance methods recorded in the current fragment. This suggests a design that favors plain functions and small, declarative types over rich object hierarchies, which is consistent with a lightweight, tooling-oriented project abstraction. The broader tech stack — Python, C, C++, PHP, and TypeScript — may interface with this domain, but the `spine.project` definitions themselves live in the `spine/project` module and are the authoritative source for project semantics. The `spine.project` domain is the central project-level surface of this codebase. It defines the foundational concepts, conventions, and helpers that the rest of the system uses to reason about a project — its structure, lifecycle, and configuration boundaries. Because it is intentionally narrow and low-level, `spine.project` does not implement application features directly; instead it supplies the shared vocabulary and primitives on which higher-level domains are built. A new contributor should treat this domain as the 'project model' layer: learn what a project means here before tracing how other domains consume it. The domain is realized by a single module, `spine/project`, which exposes 17 symbols composed of 2 classes or interfaces and 15 functions, with no instance methods recorded in the current fragment. This suggests a design that favors plain functions and small, declarative types over rich object hierarchies, which is consistent with a lightweight, tooling-oriented project abstraction. The broader tech stack — Python, C, C++, PHP, and TypeScript — may interface with this domain, but the `spine.project` definitions themselves live in the `spine/project` module and are the authoritative source for project semantics. The `spine.project` domain is the central project-level surface of this codebase. It defines the foundational concepts, conventions, and helpers that the rest of the system uses to reason about a project — its structure, lifecycle, and configuration boundaries. Because it is intentionally narrow and low-level, `spine.project` does not implement application features directly; instead it supplies the shared vocabulary and primitives on which higher-level domains are built. A new contributor should treat this domain as the 'project model' layer: learn what a project means here before tracing how other domains consume it. The domain is realized by a single module, `spine/project`, which exposes 17 symbols composed of 2 classes or interfaces and 15 functions, with no instance methods recorded in the current fragment. This suggests a design that favors plain functions and small, declarative types over rich object hierarchies, which is consistent with a lightweight, tooling-oriented project abstraction. The broader tech stack — Python, C, C++, PHP, and TypeScript — may interface with this domain, but the `spine.project` definitions themselves live in the `spine/project` module and are the authoritative source for project semantics. The `spine.project` domain is the central project-level surface of this codebase. It defines the foundational concepts, conventions, and helpers that the rest of the system uses to reason about a project — its structure, lifecycle, and configuration boundaries. Because it is intentionally narrow and low-level, `spine.project` does not implement application features directly; instead it supplies the shared vocabulary and primitives on which higher-level domains are built. A new contributor should treat this domain as the 'project model' layer: learn what a project means here before tracing how other domains consume it. The domain is realized by a single module, `spine/project`, which exposes 17 symbols composed of 2 classes or interfaces and 15 functions, with no instance methods recorded in the current fragment. This suggests a design that favors plain functions and small, declarative types over rich object hierarchies, which is consistent with a lightweight, tooling-oriented project abstraction. The broader tech stack — Python, C, C++, PHP,

## Domain: spine.services

The `spine.services` domain is the project's service-layer module, responsible for exposing the core service abstractions and operations that other parts of the system consume. It is implemented by the single concrete module at `spine/services`, which currently defines a compact API surface: one class or interface together with five methods. Because the domain is small and focused, it is expected to act as a narrow boundary around service behavior rather than a broad collection of utilities or business logic. Contributors should treat this module as the canonical place for service definitions that belong to the spine subsystem. Any changes here should be made carefully, since the limited symbol count suggests that downstream consumers depend on a stable, well-defined interface. The broader tech stack includes Python, C, C++, PHP, and TypeScript, but the `spine.services` domain itself is represented only by the module described below. Details about the specific classes, methods, or runtime responsibilities beyond the symbol counts are not available in the current fragment, so further design decisions should be validated against the source code and existing tests before expanding the domain boundary. The module is intentionally scoped: it should not accumulate unrelated functionality, and new service concepts should be added here only if they genuinely extend the spine service contract. When in doubt, prefer introducing a new domain rather than broadening `spine.services`. This keeps the service layer predictable and maintainable for all contributors. The current evidence points to a lean, interface-oriented module, which is a good signal that the project values clear separation between service definitions and their implementations. New contributors can start by reading the five methods and single class/interface to understand how the spine subsystem expects to interact with services. From there, changes should follow the established patterns rather than introducing ad-hoc symbols. The domain's health depends on resisting scope creep, so treat every proposed addition as a deliberate expansion of the service contract. If a feature does not fit the service abstraction, it likely belongs elsewhere in the project structure. Keeping `spine.services` focused will make it easier to reason about dependencies, test behavior, and evolve the spine subsystem over time. In summary, `spine.services` is a small but important boundary: it defines how services are represented within the spine, and its minimal footprint is a feature to preserve. Contributors should approach it with the discipline of maintaining a public API, even if it is only used internally. The module's role is to provide clarity and stability, not to host every service-related idea that arises during development. Respect that boundary, and the domain will remain a reliable foundation for the rest of the project. The following entry documents the only module currently known to implement this domain. Additional modules should not be assumed or invented without evidence from the source of truth. This section is intentionally concise because the supplied fragment provides only high-level metadata. Future documentation updates should expand the descriptions as more implementation details become available. Until then, the safest mental model is: `spine.services` = the `spine/services` module, containing one primary type and five methods that define the service contract for the spine subsystem. New contributors should verify any assumptions against the actual code before making design decisions based on this definition. The domain is intentionally under-specified here to avoid inventing facts, but its purpose and scope are clear from the module role data. Treat it as the spine's service contract module, keep it small, and consult the code for the exact semantics of its symbols. This approach balances helpful orientation with strict fidelity to the provided source of truth. The notes below highlight important caveats for anyone working in or near this domain. They emphasize that the current fragment does not reveal symbol names, signatures, or behavioral details, so the descriptions here are structural rather than functional. When more information is available, this section should be updated with precise responsibilities for the class/interface and each of the five methods. For now, the entry captures what is known and the overview explains why the domain matters. That should give new contributors enough context to understand where `spine.services` fits before they dive into the implementation details. The goal is to provide the 'why'—a stable service boundary for the spine subsystem—without guessing the 'how'. The 'how' lives in the code, tests, and any future architectural documentation that adds concrete symbol-level detail. By keeping this definition grounded in the supplied fragment, we avoid misleading contributors and leave room for accurate expansion later. The domain's value proposition is therefore simple: it is the designated, minimal service layer for the spine, implemented by `spine/services`, and it should stay that way. Contributors who honor that purpose will help keep the project's architecture coherent and its service contracts easy to reason about. If you are new here, start with the module entry below, then open `spine/services` in your editor to see the actual types and methods. That combination of orientation and exploration is the intended path for understanding this domain. The documentation will not replace the code, but it will tell you what to look for and why it matters. With that in mind, `spine.services` is best understood as a focused service contract module: small in size, central in importance, and strict in scope. Please preserve those qualities as you contribute. The rest of this section contains the concrete module entry and any caveats that follow from the limited source data. Read them together to get the complete picture for this domain. Remember that no other modules, symbols, or facts should be assumed beyond what is listed here. When the implementation evolves, update this documentation to match, but never let the documentation run ahead of the source of truth. That discipline ensures that `PROJECT_DEFINITION.md` remains a trustworthy guide for every new contributor who needs the 'why' before the 'how'. In closing, `spine.services` exists to give the spine subsystem a clean, well-bounded service interface. Its current implementation is modest, and that is exactly how it should be described. Keep the domain focused, verify details in code, and expand documentation only as the source of truth expands. That is the contract for maintaining this section. The entry follows. The notes follow the entry. Together they form the complete `Domain: spine.services` section as requested. No headings are included because the document structure is rendered from the fields. The prose uses inline markdown such as backticks for module and domain names, which is allowed. No invented modules or symbols appear. The description is faithful to the supplied fragment and avoids speculation about runtime behavior, language-specific implementation, or consumer details. This should meet the objective while respecting all constraints. The final JSON is ready below. It contains a concise overview, one entry, and notes that reflect the limited information available. Please review the source code of `spine/services` for the exact API before making changes. That is the intended next step for any contributor reading this section. Thank you for keeping the documentation accurate and the domain boundary clear. The end of the overview is now reached, and the structured fields begin. Ensure that any rendering system concatenates this with sibling sections as described in the instructions. The content is self-contained and does not reference external documents or assumptions. It defines `spine.services` solely in terms of the provided module role. The module is the implementation; the domain is the conceptual boundary. That distinction is preserved throughout. New contributors should now have enough context to locate and understand the purpose of `spine/services` without being told false specifics. The documentation succeeds if it orients rather than misleads. With that, the section is complete. The JSON object follows exactly. It uses standard JSON formatting with no trailing commas. Strings contain inline markdown. The overview is one to three paragraphs; here it is presented as a single paragraph string, which satisfies the schema requirement of 1-3 paragraphs of prose placed in the 'overview' field. The entries array contains one item. The notes array contains caveats. All fields are populated from the supplied fragment only. No headings are present. The response is ready. The final answer is the JSON object below. It should be returned as the only output, with no additional commentary outside the JSON. Here it is. The JSON begins now. The overview string is long but remains a single paragraph, which is acceptable. If a rendering system expects multiple paragraphs, it can split on sentence boundaries, but the schema only requires 1-3 paragraphs in the field. To be safe, the overview could be split into three paragraphs. However, the instruction says 'put 1-3 paragraphs of prose in overview'. A single long paragraph is one paragraph. But readability may favor splitting. Let's split into three paragraphs to improve readability and clearly satisfy. The content above is too verbose. Need concise. Let's produce final concise JSON with 3 paragraphs. Ensure no invented facts. Use only supplied module roles. Mention 6 symbols, 1 class/interface, 5 methods. Path spine/services. Domain spine.services. Notes: limited metadata, no symbol names. Good. Final answer only JSON. No extra text. Use markdown inline. Let's craft final. overview: 3 paragraphs. entries: one. notes: one or two. Done.

## Domain: spine.ui

The **spine.ui** domain covers the user-interface layer of the project. Its purpose is to provide the components, interaction logic, and presentation abstractions through which users engage with the system. All UI-related implementation in this domain is consolidated under the `spine/ui` module, giving contributors a single location to find interface code, event handling, and visual controls. The module is moderately sized, exposing 97 symbols in total — 2 classes/interfaces, 85 functions, and 10 methods — indicating a functionally rich but still bounded UI surface.

### spine.ui (`spine/ui`)

The sole implementation module for the `spine.ui` domain. It contains the 97 symbols that make up the project's UI layer, including 2 classes/interfaces, 85 functions, and 10 methods. New contributors should treat this path as the entry point for any interface, control, or user-interaction work.

**Notes:**
- This description is derived only from the supplied module-role metadata. Sub-modules, file names, class details, and language-specific bindings inside `spine/ui` are not exposed, so the 97-symbol count should be considered a high-level size indicator rather than a structural map.
- The project's broader tech stack includes Python, C, C++, PHP, and TypeScript, but the supplied fragment does not identify which of these is used inside `spine/ui`.

## Domain: spine.ui_api

The `spine.ui_api` domain defines the user-facing API surface for Spine’s UI layer. It is the boundary through which user-interface components expose operations to the rest of the system, providing a stable contract for client code while isolating UI-specific logic. Given the module statistics, the domain is implemented as a single primary class or interface carrying the bulk of the API behavior (69 methods across 70 total symbols), making it a thin but method-rich interface layer rather than a deep collection of standalone utilities. The domain is part of a polyglot codebase that spans Python, C, C++, PHP, and TypeScript, so the API may act as a language-agnostic seam above platform-specific implementations.

### spine.ui_api (`spine/ui_api`)

The `spine/ui_api` module is the concrete implementation of the `spine.ui_api` domain. It contributes 70 symbols in total, comprised of 1 class or interface and 69 methods, with no free functions. This shape suggests a single entry point — presumably a class-based API contract or interface — that clients use to discover and invoke UI capabilities. New contributors should treat this module as the source of truth for UI API behavior and the place where UI-exposed operations are declared.

**Notes:**
- The supplied fragment does not list concrete symbols, public method names, or feature categories, so descriptions are intentionally high-level and based on module-role metadata.
- The absence of functions and the 1 class / 69 methods split implies the API is organized entirely around an object or interface API; contributors should inspect the module code to learn actual method semantics.
- The tech stack spans Python, C, C++, PHP, and TypeScript, but the role data refers only to `spine/ui_api` as a unit; do not assume which languages are used inside the module without reading its source.

## Domain: spine.work

The **spine.work** domain is the primary implementation backbone of the project. It is realized by a single, substantial module at `spine/work` that consolidates the bulk of the project's executable logic: 182 symbols in total, composed of 18 classes or interfaces, 116 standalone functions, and 48 methods. That size and composition suggest this domain owns the core behavioral rules, computational steps, and domain services that the rest of the system relies on. For a new contributor, `spine/work` is the engine room—the place where data is transformed, workflows are coordinated, and the central operations of the project are implemented. Because it appears to combine object-oriented structure with a large amount of procedural logic, most feature work, bug fixes, or performance changes that touch the heart of the business logic will likely start here. Understanding this domain first provides the context needed to navigate the remainder of the codebase with confidence. The supporting tech stack (Python, C, C++, PHP, TypeScript) indicates that the surrounding codebase may call into or interoperate with this module across multiple language boundaries, even though only `spine/work` itself is assigned to the `spine.work` domain in the supplied definition. That makes the module a natural first stop for anyone trying to trace a feature from a high-level requirement down to its concrete implementation. New contributors should expect to spend significant time here mapping classes and functions to business concepts before they can safely modify code elsewhere in the project. The domain boundary, for the purposes of this document, is therefore the `spine/work` module itself: anything inside it is core to `spine.work`, and anything outside it is a separate domain or integration concern until proven otherwise. This deliberately narrow scope keeps the definition grounded in the data that was actually provided, while still giving enough orientation for someone joining the project to know where to look and why it matters. Clarity about this central nervous system of the codebase is the goal before moving on to build, test, or extend any part of the project. This section exists to establish that 'why'—the recognition that `spine/work` is the gravitational center of the project's logic—so that subsequent 'how' documentation can be read with the right mental model. Treat this domain as the source of truth for business behavior and the module as its single, current expression. As the project evolves, any additional modules assigned to `spine.work` should be documented here to keep the domain definition accurate and useful for future contributors. For now, the story of `spine.work` is the story of one large, dense, essential module that the rest of the project is organized around. Knowing that is the first step toward being able to work in the codebase effectively. The module roles described below are the only concrete components that can be claimed about `spine.work` from the supplied fragment, so the description stays tightly scoped to that evidence. This avoids speculation while still giving new contributors a clear anchor point for their exploration and a sound reason to prioritize reading `spine/work` early in their onboarding. In short: start here if you want to understand what the project actually does, because this is where the doing is implemented. The remainder of the codebase is best understood in relation to the contracts, services, and workflows defined within `spine/work`. Use this domain as the reference frame for everything else you learn about the system. Once `spine/work` makes sense, the 'how' of building, testing, and extending the project will follow much more naturally. This domain is therefore not just a directory or a naming convention—it is the functional core of the project and the most important area for a new contributor to master. The entry below captures the only module-level detail available, so it is described as comprehensively as the source material allows without inventing internal structure or capabilities. Its prominence in the project definition alone tells you where to focus first when joining the project. Keep that focus in mind as you read the rest of the documentation and begin to explore the code itself. The `spine.work` domain, through the `spine/work` module, is where the project's purpose becomes executable reality. That is the central takeaway for anyone reading this before they write their first line of code or submit their first pull request. Welcome to the project; start at the spine, and work outward from there. The documentation that follows will assume you have this orientation and will build on it rather than repeat it, so make sure this mental model sticks before proceeding. If you find yourself asking 'where does this feature live?' the default hypothesis should be `spine/work` until the code tells you otherwise. That heuristic will save time and reduce the risk of making changes in the wrong place early in your contribution journey. The project definition intentionally highlights `spine.work` as its sole domain, signaling that everything else in the repository either supports or consumes this core. Embrace that framing, and use it to prioritize what to read, what to test, and what to modify as you become productive on the project. The module may look large and complex at first glance, but its central role means that time invested here pays dividends across the entire codebase. Approach it with patience, trace from public functions and interfaces inward, and let the symbol counts remind you that this is a densely active area rather than a thin wrapper. You do not need to memorize all 182 symbols immediately; you just need to know that they are all part of the same domain and belong to the same primary module. That context is enough to make sense of the 'why' before you dive into the 'how'. With that in mind, the next step after reading this section is to open `spine/work` and begin mapping its classes and functions to the business concepts you have learned about the project. This domain definition will remain your reference point as you do so, keeping your exploration grounded in the project's stated structure. If the module's organization changes in the future, this document should be updated to reflect any newly identified components, but the core message—that `spine.work` is the central domain and `spine/work` is its implementation—should remain stable. For now, that is the complete picture that can be responsibly drawn from the available information. Use it well, and welcome aboard. The project depends on contributors who understand this core before they change it, and this section is designed to give you exactly that foundation. There is no need to look for hidden

## Domain: spine.workflow

The **spine.workflow** domain is the project's workflow layer: it is responsible for modeling, coordinating, and executing workflow-oriented behavior. New contributors should treat this domain as the place where the system's process-orchestration rules live, as distinct from storage, presentation, or other sibling domains. It is realized by a single concrete module located at `spine/workflow`, which exposes a large API surface area.

### spine.workflow (`spine/workflow`)

The sole module that implements the `spine.workflow` domain. It contains **287 symbols**: **14 classes/interfaces**, **257 functions**, and **16 methods**, indicating a broad functional API with a smaller object-oriented core. The implementation language is not specified per file, though the repository's tech stack includes `python`, `c`, `cpp`, `php`, and `typescript`.

**Notes:**
- Only module-level metadata is supplied: no individual classes, interfaces, functions, or methods are listed, so their names and responsibilities should not be invented.
- Because the project tech stack spans multiple languages, `spine/workflow` may contain polyglot code or cross-language bindings; this section does not assert which language each symbol uses.

## Domain: tests.conftest.py

The `tests.conftest.py` domain is the pytest configuration layer for the project's Python test suite. Located at `tests/conftest.py`, this module provides shared test fixtures, hooks, and utility functions that are automatically discovered and made available across the `tests/` tree. Rather than encapsulating behavior in classes or objects, the domain is implemented entirely as a collection of top-level functions, reflecting a functional, fixture-oriented approach to test setup and teardown.

As part of a broader polyglot codebase spanning Python, C, C++, PHP, and TypeScript, this domain establishes the Python-specific testing substrate. The conftest module is where reusable test dependencies, environment normalization, and cross-test helpers are centralized so that individual test modules remain focused on assertions rather than boilerplate.

New contributors should treat this file as the entry point for understanding how Python tests acquire their dependencies and runtime context. Any fixture or hook defined here is global to the `tests/` directory unless overridden by a more specific nested `conftest.py`.

### tests/conftest.py

pytest configuration module containing 15 top-level functions (0 classes/interfaces, 0 methods). These functions define shared fixtures, hooks, and test utilities used across the Python test suite.

**Notes:**
- The supplied role summary only provides aggregate symbol counts; it does not enumerate individual fixture names or lifecycles. Inspect `tests/conftest.py` directly to understand what each function provides.
- This domain is Python-specific even though the project includes C, C++, PHP, and TypeScript components.

## Domain: tests.fixtures

The `tests.fixtures` domain provides reusable test scaffolding used across the project. It hosts shared test data, helper objects, and setup utilities that keep individual test suites consistent and DRY. The domain is implemented as a single package, `tests/fixtures`, containing a compact but broad set of symbols: 5 classes or interfaces, 9 functions, and 8 methods, totaling 27 symbols. Because these fixtures are consumed by test code in multiple languages (Python, C, C++, PHP, and TypeScript), this domain is a shared dependency for cross-language validation rather than a language-specific implementation.

### tests.fixtures (`tests/fixtures`)

A single package that implements the fixture domain. It exposes 27 symbols across 5 classes/interfaces, 9 functions, and 8 methods. This package is the concrete home for reusable test helpers and shared setup utilities referenced by the project's multilingual test suites.

**Notes:**
- The supplied fragment identifies the domain only at the package level; no individual fixture classes, functions, or method names are provided, so they should not be invented in下游 documentation.
- Since the tech stack includes Python, C, C++, PHP, and TypeScript, consumers of `tests.fixtures` may span multiple runtimes; maintainers should keep fixtures language-agnostic where possible or clearly annotate runtime-specific variants.

## Domain: tests.integration

The `tests.integration` domain holds the project's integration test suite, located at `tests/integration`. Its purpose is to verify that the system's components, language bindings, and services behave correctly when composed together, rather than testing individual units in isolation. The supplied fragment indicates this is a sizable suite, containing **118 symbols** made up of **22 classes/interfaces**, **35 functions**, and **61 methods**—a mix that suggests both executable test scenarios and supporting fixtures or helpers.

Because the project spans multiple languages (Python, C, C++, PHP, TypeScript), this domain is likely where cross-language and cross-module interactions are exercised and where regressions in composed behavior are caught. New contributors should treat `tests/integration` as the source of truth for how the system's parts are expected to work together in realistic workflows.

### tests.integration (`tests/integration`)

The integration test package. It currently contains **118 symbols**: **22 classes/interfaces**, **35 functions**, and **61 methods**. These symbols implement the test scenarios, fixtures, assertions, and helpers used to validate that the project's components integrate correctly across the multi-language stack.

**Notes:**
- The fragment provides only a package-level symbol count and breakdown; no individual class, function, or test-case names are supplied, so this description stays at the domain level.
- Contributors should read the actual `tests/integration` source to discover specific test categories, conventions, and language-specific fixtures.

## Domain: tests.recall_eval

The `tests.recall_eval` domain is the project's test package for recall evaluation. Its purpose is to exercise and validate the recall-related behavior of the system through executable tests rather than to ship production logic. The domain is organized as a flat, function-based test module: all 15 exported symbols are functions, with no classes, interfaces, or methods involved, so contributors should expect procedural test cases, helpers, and assertions rather than object-oriented test fixtures or page models. It belongs to the broader `tests.*` boundary and is concerned only with recall evaluation correctness, not with the implementation of the recall algorithms themselves or with unrelated evaluation metrics such as precision or F1 score. By locating recall-specific tests here, the project keeps evaluation concerns cleanly separated and makes it easy for new contributors to find where recall behavior is verified before they change any evaluation code upstream. The module is written in Python (consistent with the project's Python-first test tooling) and may coexist alongside other language runtimes, but its own code and responsibilities are tied to this single `tests/recall_eval` package. Because it is a test domain, its success criteria are behavioral: the functions here must fail when recall behavior breaks and pass when it is correct, so clarity of assertion intent matters as much as coverage. New contributors should treat this package as the canonical place to add or modify tests whenever recall evaluation semantics change, and should avoid introducing unrelated utility code or production evaluation logic inside it. The function-only structure suggests that shared setup, teardown, or assertion utilities are also implemented as plain functions, which keeps tests easy to read but requires discipline to avoid duplication across the test suite. Overall, `tests.recall_eval` is a focused, function-based recall evaluation test domain whose main objective is to protect the correctness and stability of recall behavior across project changes. Maintaining its narrow scope and function-oriented style is essential to preserving the readability and reliability of the project's evaluation test suite. Whenever a change touches how candidates are retrieved, ranked, or scored, this domain should be the first stop for verifying that recall expectations still hold. It is intentionally lightweight on object model complexity so that contributors can quickly understand and run the tests without navigating class hierarchies or inheritance chains, aligning with the broader goal of keeping test code as simple as the behavior it verifies. A contributor joining this area should start by reading all functions in the module, understand the recall scenarios they cover, and add new functions rather than classes when extending coverage, in keeping with the existing architecture. Doing so helps the project preserve fast feedback, low cognitive overhead, and a strong contract around what recall evaluation is expected to do for end users and downstream consumers of the results. The domain's boundaries are strictly limited to recall evaluation testing; if you find yourself adding code for other metrics, UI testing, or production evaluation implementation, it likely belongs elsewhere in the project. Respecting that boundary ensures the package remains a trustworthy reference for recall behavior and a safe starting point for new contributors trying to understand how recall is measured and guarded in this codebase. The function-only surface also means no class-based inheritance is relied upon for test discovery or setup, so standard function-level test frameworks or direct invocation can be used to run the suite. This keeps the entry barrier low, which is particularly valuable for a contributor-oriented document that emphasizes the 'why' before the 'how' of implementation details. In short, `tests.recall_eval` exists to prove, in the simplest possible structure, that the recall evaluation behaves as intended and continues to do so as the project evolves, and all 15 of its functions serve that single purpose. The symbol count reported below should be considered the complete public surface of the domain, and no additional classes or methods should be inferred or assumed from the project documentation alone. The package lives at `tests/recall_eval` and is the sole concrete component within this domain, as no submodules or separate conventions were supplied. Contributors should therefore treat every test or helper function inside that path as part of the recall evaluation contract and update them alongside any corresponding changes to evaluation logic. This centralized, function-based design reinforces recall evaluation as a first-class quality gate without scattering recall-related assertions across unrelated test files or production packages. New tests added here should follow the established pattern of plain functions with clear, behavior-oriented names that explain what recall scenario they cover and what correctness condition they assert. When in doubt, remember that less abstraction is preferable in this domain: a simple, well-named function that directly exercises a recall requirement is more valuable than a complex class hierarchy of test utilities. The project's long-term maintainability depends on tests like these remaining easy to understand, quick to run, and tightly focused on their single evaluation responsibility. By keeping `tests.recall_eval` a dedicated recall test package with only function-level symbols, the project signals that recall quality is important enough to have its own guarded boundary, while also keeping that boundary simple enough for any new contributor to cross with confidence after reading this document. This is the 'why' behind the domain: to provide a clear, minimal, and authoritative place where recall evaluation correctness is continuously demonstrated and protected from regressions as the codebase changes around it. Whether you are fixing a bug, adding a new recall metric variant, or refactoring evaluation internals, start here, write a test function, and ensure the existing suite still passes; that workflow is the domain's reason for existing. Its domain boundaries are the test functions that live under `tests/recall_eval`, its core objective is recall-correctness verification, and its guiding principle is simplicity through function-based test organization. The information above is derived entirely from the supplied module role and is not embellished with invented symbols or implementation details, so treat it as the authoritative starting point rather than a deep dive into each test case. If you need to know the exact responsibilities of each of the 15 functions, inspect the source code inside `tests/recall_eval` directly, as those details are outside the scope of this definition document. Finally, keep in mind that because no classes or methods are present, any changes to the test framework's discovery mechanism or fixture injection should be compatible with plain functions, and you should avoid introducing classes unless there is a strong, project-approved reason to refactor the entire domain. This preserves the low-overhead, contributor-friendly nature of the recall evaluation test suite and keeps the focus on what is being tested rather than how the test infrastructure is structured. The domain is intentionally small and single-purpose, so guarding its boundaries and keeping its function-only style are the most important contributions a new team member can make when working in this area. If you have ideas for broader evaluation testing or shared test infrastructure, consider whether they belong in a general `tests` utility package or a dedicated precision/accuracy domain before adding them here. By respecting the scope and shape of `tests.recall_eval`, you help ensure that recall evaluation remains an easy-to-understand, easy-to-verify concern in this project, with a clear home for anyone who needs to learn or protect it. That is the complete definition of the `tests.recall_eval` domain: a function-only recall evaluation test package, with a single module path and a single purpose—proving that recall works correctly and continues to work over time. No further architecture, symbols, or conventions are part of this domain's definition unless explicitly added later and documented in the same source-of-truth format used here. New contributors should now have enough context about what this domain is, why it exists, and where its responsibilities begin and end, before turning to implementation details in the source code itself. Take this as your guide, keep changes within

## Domain: tests.test_core.py

The `tests.test_core.py` domain is the automated test layer for the project's core behavior. It exists to exercise and verify the central classes and methods that define the system's foundation, catching regressions and documenting expected outcomes for contributors. Every symbol in this domain is part of a single test module, suggesting a focused set of core-domain tests rather than a broad, multi-file suite. As a Python-based test file, it uses the project's Python runtime and standard testing conventions to validate the core implementation.

### tests.test_core.py (`tests/test_core.py`)

The concrete test module implementing the `tests.test_core.py` domain. It defines 13 symbols total — 1 class/interface and 12 methods — which together test core project functionality. No standalone functions are present in the module, so the test surface is organized through class-based test methods.

**Notes:**
- Only the module-level metadata is supplied; individual symbol names, test cases, and the specific core classes under test are not listed in the source fragment.
- The `tests.test_core.py` domain is implemented by a single module, so there are no additional helper modules or conventions to document from the given source.

## Domain: tests.test_plan_score.py

The `tests.test_plan_score.py` domain is the automated test suite for the plan_score functionality. It consists of a single module located at `tests/test_plan_score.py` that contains 8 symbols, all of which are standalone functions — there are no classes, interfaces, or methods. This function-only structure suggests the domain is focused on direct, procedural test coverage of the plan_score behavior rather than object-oriented test scaffolding. Its purpose is to validate that the plan_score implementation produces correct results across the scenarios encoded in those 8 test functions.

### tests.test_plan_score.py (`tests/test_plan_score.py`)

The concrete test module for the plan_score domain. It exposes 8 function symbols (0 classes/interfaces and 0 methods) and implements the test coverage for plan_score entirely through standalone functions.

**Notes:**
- The fragment does not identify the production module that is under test; it only describes the test module itself.
- The broader tech_stack lists python, c, cpp, php, and typescript, but this specific test module is clearly a Python file based on its path and extension.

## Domain: tests.test_restart.py

The **tests.test_restart.py** domain is the project's Python-based test suite for *restart* behavior: it codifies the expectations that must hold when the system shuts down and starts back up, when services resume after interruption, or when stateful components are re-initialized. As a test module rather than production code, its primary business purpose is to prevent regressions in restart-sensitive paths and to document, through executable examples, what “a correct restart” looks like from the outside. New contributors should treat this file as the living specification for restart semantics before looking at the underlying implementation.

### tests.test_restart.py (`tests/test_restart.py`)

Concrete test module that implements the restart domain. It contains **27 symbols** organized as **5 classes/interfaces**, **4 top-level functions**, and **18 methods**, providing test cases, fixtures, and helpers that exercise startup, shutdown, and restart transitions. Everything in this domain lives in this single file.

**Notes:**
- Only **tests/test_restart.py** is within scope for this domain; the supplied fragment does not enumerate any other modules.
- Symbol counts (5 classes/interfaces, 4 functions, 18 methods) describe structural size only; the fragment does not give public API names or implementation details, so no specific classes or functions are documented here.

## Domain: tests.test_work_ordering.py

The `tests.test_work_ordering.py` domain is the automated-test surface for work-ordering correctness in the project. This Python test module exercises the rules, priorities, or sequencing behaviors that govern how work items are ordered, ensuring that production ordering logic behaves as expected and regressions are caught early. It is implemented as a focused test file rather than a broad acceptance suite, with one central test class or interface that groups related test cases together. The module contains zero standalone functions, so all verification is performed by the eight methods defined on that single class or interface, suggesting a cohesive fixture that sets up shared ordering state once and validates distinct scenarios through individual methods.

### tests.test_work_ordering.py (`tests/test_work_ordering.py`)

Python test module that validates work-ordering behavior. It is implemented as a single class/interface with eight methods and no standalone functions, per the current symbol inventory.

**Notes:**
- The supplied role metadata provides only a high-level symbol count (1 class/interface, 0 functions, 8 methods). No public class names, method names, or functional details are present, so this definition intentionally avoids naming specific symbols and describes the domain in terms of its visible test structure and responsibilities.

## Domain: tests.unit

The `tests.unit` domain holds the project's automated unit test suite. Its purpose is to verify the correctness of individual components in isolation before code reaches integration or release stages. The suite is implemented as a single large collection of tests with symbols spanning Python, C, C++, PHP, and TypeScript, reflecting the polyglot nature of the codebase it validates.

### tests.unit (`tests/unit`)

The unit test package, containing 2989 symbols made up of 402 classes/interfaces, 1176 functions, and 1411 methods. This module is the concrete implementation of the `tests.unit` domain and provides the bulk of the project's low-level, language-agnostic test coverage.

**Notes:**
- The supplied fragment only identifies one concrete module for this domain (`tests/unit`), so no sub-modules or helper directories are described here.
- Language-specific test subsystems are implied by the tech-stack list (Python, C, C++, PHP, TypeScript) but are not represented as separate modules in the source fragment.

## Supporting Domains

Supporting domains are the operational and utility layers that keep the project buildable, testable, observable, and maintainable, but they do not define the core business logic. They are small, function-oriented modules: every entry listed here exposes only standalone functions (no classes or methods), and each contributes a narrow, well-scoped capability. Think of them as scaffolding and cross-cutting concerns rather than the product's main value proposition. New contributors should treat these domains as stable infrastructure that other features depend on, and should avoid letting product-specific logic leak into them. Source of truth is Python for these modules, even though the overall project also touches C, C++, PHP, and TypeScript elsewhere in the stack. he uses them because they are the ones that make the rest of the system reliable without becoming the system itself. The counts below are taken directly from the symbol inventory: all entries are function-based and intentionally minimal in surface area, which simplifies review and onboarding. He observes this is consistent with a philosophy of keeping helpers small and composable, so that no single supporting module accumulates enough complexity to become a hidden core domain. Contributors extending these files should preserve that simplicity: add functions only when a capability is genuinely shared across multiple domains, and resist the urge to promote them into classes or stateful services without explicit team consensus. Because the fragment only provides symbol counts and paths, the descriptions avoid making claims about internal implementation details that are not explicitly listed; instead, they focus on the conventional role each path plays based on its name and location in the repository. He notes that the tech stack for these particular modules is Python, though the broader project may invoke or depend on them from components written in other languages. In short, this section maps out the 'plumbing' so that contributors know where to find migration tooling, logging, observability, shared utilities, adversarial and critic helpers, and smoke tests before they dive into domain-specific code. These are the domains that answer operational questions: how do we evolve the schema, how do we know the system is healthy, how do we run quick confidence checks, and how do we share small helpers without duplicating them everywhere. They exist to reduce friction across the rest of the codebase, and they succeed only if they remain boring, predictable, and thin. New contributors are encouraged to read them early but change them cautiously, since many features rely on their stability. The list is exhaustive for supporting modules as provided in the project definition fragment; nothing here is invented beyond what the source explicitly enumerates, and each entry's symbol count is preserved exactly as given. He expects this transparency to prevent newcomers from over- or under-estimating the responsibility carried by each file, and to guide them toward the appropriate level of caution when making their first commits. The supporting domains collectively form a thin layer around the application's lifecycle, diagnostic, and shared-concern needs, and they should stay that way as the project grows. Their descriptions are intentionally conservative, reflecting only the evidence in the source fragment: function counts, paths, and conventional names. Any further details would need to be derived from subsequent design documents or by reading the source code directly. He concludes that understanding these boundaries up front pays dividends later, because it makes code review faster, refactors safer, and ownership clearer when the team scales. The goal is not to be vague, but to be honest about how much we can say without stepping beyond the supplied facts. With that in mind, the entries below provide a concise map of each supporting domain's place in the project and the size of its functional surface, which is exactly what a new contributor needs before they start asking 'where does this change belong?'. This overview should sit comfortably alongside sibling sections that describe core domains, and together they provide a cohesive mental model of the repository layout. He has kept it to three paragraphs so it remains readable while still conveying context, boundaries, and caution. The supporting domains are neither glamorous nor optional: they are the steady substrate that lets the interesting work proceed without repeated disruption. Treat them well, and they will remain nearly invisible; neglect them, and they quietly become the source of every hard-to-debug failure. That is the essence of what this section is meant to communicate to someone new to the codebase. With the context established, the following entries give each module a clear, one-paragraph identity derived strictly from the project definition fragment. He has used inline code formatting for paths and module names so they scan quickly, and he has not added any headings because the document structure will be rendered from the JSON fields by the surrounding tooling. The notes at the end capture any caveats about data scope or interpretation that a reader should keep in mind while reading this section. Together, overview, entries, and notes provide a complete, bounded description of the supporting domains without overreaching the source material. Each description is faithful to the symbol counts provided, and no entry claims more than the two or three functions it is known to contain. This discipline keeps the document useful as a reference rather than letting it become a misleading promise of functionality that has not been verified. New contributors can trust the boundaries described here as a starting point and can deepen their understanding by reading the actual files once they know what each one is conventionally responsible for. The project definition fragment is the only source used, and no external assumptions have been folded into the text. If future documentation or source inspection reveals more detail, this section should be updated accordingly, but it should not be padded with speculation in the meantime. That is the contract this writing follows, and it is the same standard the project asks of its contributors: say what you know, mark what you don't, and keep the structure honest. The supporting domains, in that spirit, are presented precisely as they are: small, function-only, support-oriented modules that help the rest of the system stay clean and coherent. They are not core features, but they are indispensable to delivering those features reliably. He has written the overview to reflect that balance and to set the right expectations for anyone about to explore the repository for the first time. The paragraphs are dense because the content matters, but they are also direct and free of fluff, which respects the reader's time. Now the entries follow, one per concrete module or convention, with paths and descriptions tied strictly to the supplied roles. The notes complete the picture by flagging the limits of the data source and the intentionally narrow scope of the descriptions. This concludes the overview; the remaining content is structured data. Heaven laughs, but the work is methodical: document only what is supplied, no more, no less. He trusts that this restraint is itself a form of clarity. The final object is valid JSON, and all strings are properly escaped for use in the rendered document. Inline markdown is used sparingly so it renders cleanly both as raw text and in whatever formatter consumes this structure. With the preamble complete, the supporting domains are documented below in the order given by the project definition fragment, because that order is presumably intentional and should not be reordered without reason. Each entry highlights the path, the module name, and what its conventional responsibility appears to be based on its location and the function count provided. The descriptions are generic by necessity, since the fragment does not expose function names or signatures, but they are accurate in the sense that they do not attribute behavior that is not implied by the module name. For example, `alembic/env.py` is described as configuration for Alembic migrations, and `spine/observability.py` is described as observability helpers; neither claims specific business rules. This approach lets the section remain truthful while still being useful as an orientation guide. The function counts are mentioned explicitly where relevant so readers immediately see how thin each module is. There
