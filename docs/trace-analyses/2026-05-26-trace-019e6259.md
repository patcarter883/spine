# LangSmith Run Analysis — `019e6259-f4bd-7483-a339-36234432e143`

**Date:** 2026-05-26
**Revision:** `04a4605-dirty` (includes both recent commits: research-manager hydration + SPECIFY pre_research_gate short-circuit)
**Duration:** 10m 22s — **outcome: failed** (`GeneratorExit` at root; root cause = `CriticalContractFailure` in `specify`/`save_artifacts`)
**Tokens:** 2,738,173 total (2.72M prompt, 15K completion, **0 cache reads**)

## What happened, end-to-end

1. **Classification ran** — `task_category="Generic"`, `classification_confidence=0.85`, reasoning OK.
2. **Pre-research gate fired but did NOT short-circuit**, despite confidence 0.85 ≥ `recall_gate_confidence=0.75`. Reason: recall only returned **3 hits** but `recall_gate_min_hits=5` (`spine/config.py:107-108`). Gate fall-through logic at `spine/workflow/subgraphs/exploration_subgraph.py:144` requires *both* conditions.
3. **Three research_manager rounds** ran (rounds 1→2→3), each spawning `explore` topics in parallel. Many sub-explores failed:
   - `Input validation error: 'pattern' is a required property`
   - `Input validation error: 'name' is a required property`
   - One `No streaming chunk received for 120.0s` (model-side stall)
4. **Synthesize succeeded** — agent called `write_specification(...)` and the tool returned `specification.md written to .spine/artifacts/e57bba8d/specify/specification.md (2806 chars)`.
5. **`save_artifacts` then failed** with `CriticalContractFailure: specification.json does not exist — the specify agent did not produce structured output via write_specification`. The agent wrote `.md`, the contract checks for `.json` → mismatch.

## Impact of the recent changes

**`fix(exploration): hydrate research manager with recall + classification` (04a4605)** — partially working:
- Hydration is live: `classification_confidence=0.85`, `classification_reasoning`, and `retrieved_context` (3 chunks) are all populated in the research_manager input on round 1. The fix did its job.
- Gate didn't engage because recall yielded only 3 hits. Either ingestion is sparse for "configuration / setup / UI" topics, or `recall_gate_min_hits=5` is too strict for this codebase. Loosening to 3 (or making it scale by confidence) would have skipped ~10 minutes of exploration here.

**`SPECIFY pre_research_gate short-circuits high-confidence runs` (6277bdf)** — wired correctly, but unreached on this run for the reason above. The decision path was Classify → recall → research_manager (3 rounds), exactly as expected when gate falls through.

**`SmallCode IMPLEMENT refactor` (9299242)** — not exercised; trace failed in SPECIFY before any IMPLEMENT.

## Cost / efficiency observations

- **2.72M prompt tokens, 0 cache reads** — extremely expensive. The explore agents are passing huge prompts uncached. Most of the cost is concentrated in two `explore` calls (386K and 248K prompt tokens each).
- A handful of explore subtasks burned ~120s each on stalled streaming before erroring → bulk of wall-clock waste.
- Repeated topics across rounds: "test_path_different_phases / _artifact_path" appears twice in topics — research_manager is asking for things it already has in `retrieved_context`. The hydration delivers context but the manager prompt may not be reading it before proposing new topics.

## Concrete bugs surfaced

1. **Contract/extension mismatch** — `save_artifacts` for SPECIFY asserts `specification.json` exists, but the `write_specification` tool persists `specification.md`. This is the actual failure of the run. Either rename the tool output or relax the contract to accept `.md`.
2. **Explore sub-agent tool-schema bug** — `'pattern' is a required property` / `'name' is a required property` repeatedly, suggesting the dispatcher is sending malformed tool args to the explore worker. Worth tracing one of the failed `explore` runs to see which tool's input schema is unhappy.
3. **No cache hits** — worth checking whether prompt-caching is enabled on the explore agent's model wrapper; this trace is wildly expensive without it.

## Suggested next moves

- Fix the `.json` vs `.md` contract — that unblocks the run end-to-end.
- Investigate the explore worker's `'pattern'/'name' is a required property` validation error (likely a tool-schema regression).
- Consider lowering `recall_gate_min_hits` (e.g. 3) or making the gate trigger on `confidence ≥ 0.85` regardless of hit count, to actually exercise the new short-circuit.
- Verify prompt caching is enabled on the explore model — biggest cost lever.
